# FastAPI i serwer
fastapi>=0.109.0
# Starlette < 0.46: starsza sygnatura TemplateResponse(name, context). Od 0.46 wymagana
# nowa sygnatura TemplateResponse(request, name, context) - wymagaloby przepisania ~8 wywolan
# w app/main.py, planowane jako osobny task.
starlette<0.46
pydantic-settings>=2.0.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Szablony i pliki statyczne
jinja2>=3.1.0
aiofiles>=23.0.0

# Baza danych
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Autentykacja
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
pydantic[email]>=2.0.0

# HTTP client (dla reCAPTCHA)
httpx>=0.26.0

# Konwersja XML -> XLSX (z projektu głównego)
lxml>=4.9.0
openpyxl>=3.1.0
